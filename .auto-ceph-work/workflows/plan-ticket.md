# Plan Ticket Workflow

- Read reviewed `02_CONTEXT.md`.
- Add git preparation steps.
- Finalize the verification criteria when the ticket touches an API.
- Apply repo-specific rules such as `remote-ceph-admin`.
- Ensure Jira status is `IN PROGRESS`.
- Write `03_PLAN.md`.
- Sync Jira note to `계획`.
- Return a final `<stage_result>` block with all required fields from `stage-result-format.md`.
